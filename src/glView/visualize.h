#ifndef _VISUALIZE_H
#define _VISUALIZE_H

// opencv(or other libs) config options
typedef struct{

}visual_config;

// functions interfacing with opencv
void update_window();
void flush_window();

#endif